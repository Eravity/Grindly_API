services:
  - type: web
    name: grindly-api
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: node debug-render.js && node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
