services:
  - type: web
    name: grindly-api
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run prod
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
